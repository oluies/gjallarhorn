name: CI

on:
  # Fire on every push to any branch (feature branches need CI feedback
  # before PR review; the cross-repo work with Neverlur means
  # boundary-crossing changes can't be verified locally on Apple Silicon
  # due to the pre-existing vuvuzela.io/crypto/bn256 assembly limitation,
  # so CI is the first verification surface).
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: build & test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go: ['1.25']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go ${{ matrix.go }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          cache: true

      - name: Verify modules
        run: go mod download

      - name: Check gofmt
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then
            echo "::error::gofmt found unformatted files:"
            echo "$out"
            exit 1
          fi

      - name: Build
        run: go build ./...

      - name: Vet
        continue-on-error: true
        run: go vet ./...

      - name: Test (unit)
        # Everything EXCEPT the integration-test directory. The unit
        # tests cover the convo / coordinator / mixnet packages and
        # the upcoming internal/testharness package once landed.
        #
        # continue-on-error remains TRUE because internal/testharness
        # has a pre-existing -race-detected data race in the convo
        # coordinator (goroutine racing on srv.rounds map between
        # loop() and updateConfigLoop()). The mixnet "cardinality
        # violation" was a separate bug; that one is FIXED in this PR
        # (mixnet/mixnet.go AddOnions now calls SendAndClose). The
        # race is the next thing to fix before we can flip this to
        # hard-fail.
        #
        # Tracked: https://github.com/oluies/gjallarhorn (TODO open
        # issue once Block C lands).
        continue-on-error: true
        run: |
          pkgs=$(go list ./... | grep -v '/e2e$')
          go test -race -count=1 -timeout 5m $pkgs

      - name: Test (e2e)
        # The Phase A integration tests + the hybrid-carry-through
        # subtests + the no-classical-session-key static check.
        #
        # Hard-failing as of Block B: TestNoClassicalSessionKey is
        # live (AST walker, Principle III enforcement). The
        # TestE2EFirstMessage scaffold and ClassicalCompromise /
        # PQCompromise subtests t.Skip pending the coordinator
        # data-race fix + a TestClient.Start() helper — see the
        # tests' comments for the unblocking checklist.
        run: |
          if [ -d ./e2e ]; then
            go test -count=1 -timeout 5m ./e2e/...
          else
            echo "no e2e/ directory yet; skipping"
          fi

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    # continue-on-error while the inherited code still has findings the
    # path-except rule must catch but may miss as the codebase evolves.
    # Flip to hard-fail once internal/testharness/ and e2e/ are the
    # only PQ-touched paths and the exclusion is known-stable.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: latest
